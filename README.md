# The frame Framework 

Frame - это мультифункциональный фреймворк сочетающий в себе концепций, которые являются отдельными пакетами. 

| Пакет | Концепция | Термины | версия |
| :--- | :--- | :--- | :--- |
| Frames | Создание изолированных контекстов для исполнения кода и конфигурации. | Frame, Framing, Framefile, Superglobal | 0.4.12 |
| Nets | Работа с криптографией, белый/серый хакинг и интернет безопасность. | Пока нет | 0.1.1 |

## 🚀 Подробное описание концепций
### 🖼 Frames
Концепция призвана облегчить перенос и сереализацю кода, а так же конфигурирование.

**Главные функции** - 
  - **Framer** - Низкоуровневая реализация фреймов, над которой строятся асбтракции.
  - **Var, Get, Exec, Return, Code, SystemOp** - Низкоуровневые функции для работы с Framer напрямую.
  - **Frame** - Основной Api для работы c концепцией на высоком уровне.
  - **FramesComposer** - Позволяет объеденять фреймы в единую систему для быстрой работы.

**Термины** -
  - Frame - An isolated execution space with its own variables and code. Can interact with other contexts.
  - Framing - creating a local environment with superglobal variables.
  - Superglobal is the state of an object when it does not depend on the context. Roughly speaking, a global frame.
  - Framefile is a binary frame image that can be saved and loaded.

**Пример использования показывающий наглядно пользу концепции** -

Предположим что у нас есть файл конфигуразии простой нейросети -
```python
koeff = 0.5
learning_rate = 0.04
test_input = 'test'
epochs = 2000
batch_size = 256
def not_for_import():...
```
Значит что в основном файле придется прописывать - 
```python
from config import test_input, epochs, batch_size, koeff, learning_rate
print(test_input)
```
Это же невероятно неудобно! При импорте приходится помнить названия переменных и писать большую строку для их импорта.
Но вот как этот код выглядит при использование концепции - 
```python
from frame import Frame
sgc = Frame() # superglobal context
sgc.Var('koeff', 0.5, 'float')
sgc.Var('learning_rate', 0.04, 'float')
sgc.Var('batch_size', 256, 'int')
sgc.Var('epochs', 2000, 'int')
sgc.Var('test_input', 'test', 'str')
def not_for_import():...
```
Теперь в основном файле мы просто импортируем контекст - 
```python
from config import sgc
print(test_input)
```
Это в разы проще и чище! 

### 🌐 Nets
